package Domian;

import java.util.Date;

import lombok.Data;

@Data
public class User {
	private Long id;
	private String username;
	private Integer age;
	//@DateTimeFormat(pattern = "yyyy-MM-dd")//设置时间格式，当使用DateUtils 类增强之后，就可以不用在每一个日期上面贴上注解啦
	//@JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss", timezone = "GMT+8")
	private Date hiredate;//入职时间
}
